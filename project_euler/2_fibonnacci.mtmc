include "../lib/std.mtmc";


fibonacci(x, y)[none]
{
    0 -> sum;
    x + y -> s;
    if (y < 4000000)
    {
        !fibonacci(y, s) -> f;
        sum + f -> sum;
        if (s % 2 = 0)
        {
            s + sum -> sum;
        };
    };
    return sum;
};

!print(
    2 + !fibonacci(1, 2)
);